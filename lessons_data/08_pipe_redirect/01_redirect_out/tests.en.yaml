initial_filesystem:
  /home/user:
    type: directory
    children:
      documents:
        type: directory
        children:
          readme.txt:
            type: file
            content: 'hi

              '
      notes.txt:
        type: file
        content: 'memo

          '

initial_cwd: /home/user

solution_commands:
  - "ls > filelist.txt"

tests:
  - name: "ls output saved to filelist.txt"
    description: "File content after redirect"
    type: command_sequence
    steps:
      - command: "ls > filelist.txt"
        expected_output: ""
      - command: "cat filelist.txt"
        expected_output: |
          documents
          filelist.txt
          notes.txt
